package capabilities

// LoadTarget is the metadata-only VM loader. Its behavior is implemented in
// the capabilities implementation task.
func LoadTarget(name string) (Target, error) {
	return Target{}, nil
}
