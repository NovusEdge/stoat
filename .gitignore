# build artifacts
/vendor/
*.iso
*.qcow2
/stoat
/installer

.env

# the installer copies to a temp file in the destination dir and renames, so a
# kill mid-copy can orphan one. Only lands here if $PREFIX points into the repo.
.stoat-*

# agent / planning cruft — local only. Task plans are throwaway; durable
# architecture decisions belong in docs/design/, which IS tracked.
docs/superpowers/
docs/specs/
context/
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.aider*
.superpowers/

# session resume state, local only
/CHECKPOINT.md

# local cloud-init hardware-gate test
/.cloudinit-test/
.worktrees/

# graphify knowledge graph, rebuilt locally
graphify-out/
