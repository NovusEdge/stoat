# build artifacts
/vendor/
*.iso
*.qcow2
/stoat
/installer

.env

# the installer copies to a temp file in the destination dir and renames, so a
# kill mid-copy can orphan one. Only lands here if $PREFIX points into the repo.
.stoat-*

# agent / planning cruft and internal notes — local only. Design docs, specs,
# and QA scripts stay out of the published site and off GitHub.
docs/superpowers/
docs/specs/
docs/qa/
docs/recipe-authoring-spec.md
docs/recipe-spec-v2.md
docs/design/
context/
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.aider*
.superpowers/

# session resume state, local only
/CHECKPOINT.md
/HANDOFF.md

# local cloud-init hardware-gate test
/.cloudinit-test/
.worktrees/

# graphify knowledge graph, rebuilt locally
graphify-out/
